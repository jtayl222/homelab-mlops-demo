apiVersion: v1
kind: ConfigMap
metadata:
  name: serve-template
data:
  serve.py: |
    # Template will be processed by kustomize replacements
    if __name__ == "__main__":
        import uvicorn
        uvicorn.run(app, host="0.0.0.0", port=MODEL_SERVING_PORT)
